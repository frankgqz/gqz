import { t as require_react } from "./react.js";
import { t as require_jsx_runtime } from "./react_jsx-runtime.js";
//#region node_modules/@gqz/theme/src/theme.ts
var themeOrder = [
	"wood",
	"dark",
	"sky",
	"matcha"
];
var themes = {
	wood: {
		bg: "#F5F0E8",
		text: "#5D4E3A",
		subtext: "#8B7355",
		accent: "#8B7355",
		glow: "radial-gradient(circle, rgba(139,115,85,0.15) 0%, transparent 70%)",
		buttonStart: "#6B5344",
		buttonEnd: "#8B7355",
		buttonShadow: "0 8px 32px rgba(139,115,85,0.36), 0 2px 8px rgba(0,0,0,0.28), inset 0 1px 0 rgba(255,255,255,0.12)",
		buttonShadowPressed: "0 2px 12px rgba(139,115,85,0.3), inset 0 1px 0 rgba(255,255,255,0.08)",
		particleHues: [
			{
				hue: [15, 35],
				sat: [50, 80]
			},
			{
				hue: [25, 45],
				sat: [70, 90]
			},
			{
				hue: [0, 20],
				sat: [65, 90]
			},
			{
				hue: [45, 70],
				sat: [60, 90]
			},
			{
				hue: [80, 140],
				sat: [40, 80]
			}
		]
	},
	dark: {
		bg: "#0D0E14",
		text: "#4b5068",
		subtext: "#2d3148",
		accent: "#6366f1",
		glow: "radial-gradient(circle, rgba(99,102,241,0.12) 0%, transparent 70%)",
		buttonStart: "#4F46E5",
		buttonEnd: "#818CF8",
		buttonShadow: "0 8px 40px rgba(99,102,241,0.5), 0 2px 16px rgba(0,0,0,0.35), inset 0 1px 0 rgba(255,255,255,0.15)",
		buttonShadowPressed: "0 2px 16px rgba(99,102,241,0.6), inset 0 1px 0 rgba(255,255,255,0.12)",
		particleHues: [
			{
				hue: [200, 260],
				sat: [70, 100]
			},
			{
				hue: [220, 280],
				sat: [70, 100]
			},
			{
				hue: [250, 290],
				sat: [70, 100]
			},
			{
				hue: [260, 300],
				sat: [70, 100]
			}
		]
	},
	sky: {
		bg: "#FFFFFF",
		text: "#5D7090",
		subtext: "#8090A8",
		accent: "#8090B5",
		glow: "radial-gradient(circle, rgba(180,190,220,0.08) 0%, transparent 70%)",
		buttonStart: "#6080A8",
		buttonEnd: "#90B0D5",
		buttonShadow: "0 8px 32px rgba(96,128,168,0.3), 0 2px 8px rgba(96,128,168,0.2), inset 0 1px 0 rgba(255,255,255,0.15)",
		buttonShadowPressed: "0 2px 12px rgba(96,128,168,0.4), inset 0 1px 0 rgba(255,255,255,0.1)",
		particleHues: [
			{
				hue: [200, 230],
				sat: [20, 40]
			},
			{
				hue: [230, 270],
				sat: [15, 35]
			},
			{
				hue: [250, 280],
				sat: [20, 40]
			}
		]
	},
	matcha: {
		bg: "#D9CFC4",
		text: "#5D6855",
		subtext: "#7A8B75",
		accent: "#7A8B75",
		glow: "radial-gradient(circle, rgba(122,139,117,0.10) 0%, transparent 70%)",
		buttonStart: "#5A6B55",
		buttonEnd: "#7A8B70",
		buttonShadow: "0 8px 32px rgba(90,107,85,0.3), 0 2px 8px rgba(90,107,85,0.15), inset 0 1px 0 rgba(255,255,255,0.12)",
		buttonShadowPressed: "0 2px 12px rgba(90,107,85,0.3), inset 0 1px 0 rgba(255,255,255,0.08)",
		particleHues: [
			{
				hue: [20, 45],
				sat: [20, 50]
			},
			{
				hue: [90, 130],
				sat: [20, 50]
			},
			{
				hue: [140, 180],
				sat: [15, 45]
			}
		]
	}
};
//#endregion
//#region node_modules/@gqz/theme/src/useTheme.tsx
var import_react = require_react();
var STORAGE_KEY = "gqz-theme";
var ThemeContext = (0, import_react.createContext)(null);
function useTheme() {
	const ctx = (0, import_react.useContext)(ThemeContext);
	if (!ctx) throw new Error("useTheme must be used inside ThemeProvider");
	return ctx;
}
//#endregion
//#region node_modules/@gqz/theme/src/ThemeProvider.tsx
var import_jsx_runtime = require_jsx_runtime();
function ThemeProvider({ children }) {
	const [activeTheme, setActiveThemeState] = (0, import_react.useState)("wood");
	(0, import_react.useEffect)(() => {
		const stored = localStorage.getItem(STORAGE_KEY);
		if (stored && [
			"wood",
			"dark",
			"sky",
			"matcha"
		].includes(stored)) setActiveThemeState(stored);
	}, []);
	(0, import_react.useEffect)(() => {
		const colors = themes[activeTheme];
		const root = document.documentElement;
		root.style.setProperty("--theme-bg", colors.bg);
		root.style.setProperty("--theme-text", colors.text);
		root.style.setProperty("--theme-subtext", colors.subtext);
		root.style.setProperty("--theme-accent", colors.accent);
		root.style.setProperty("--theme-glow", colors.glow);
		root.style.setProperty("--theme-btn-start", colors.buttonStart);
		root.style.setProperty("--theme-btn-end", colors.buttonEnd);
		root.style.setProperty("--theme-btn-shadow", colors.buttonShadow);
		root.style.setProperty("--theme-btn-shadow-pressed", colors.buttonShadowPressed);
		root.setAttribute("data-theme", activeTheme);
	}, [activeTheme]);
	const setActiveTheme = (theme) => {
		setActiveThemeState(theme);
		localStorage.setItem(STORAGE_KEY, theme);
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(ThemeContext.Provider, {
		value: {
			activeTheme,
			setActiveTheme,
			themeColors: themes[activeTheme]
		},
		children
	});
}
//#endregion
//#region node_modules/@gqz/theme/src/ThemeToggle.tsx
var themeIcons = {
	wood: "🍂",
	dark: "🌙",
	sky: "🌤️",
	matcha: "🍵"
};
var toggleBg = {
	wood: "#E8E0D5",
	dark: "#1a1d2e",
	sky: "#E8EBF5",
	matcha: "#E0DDD5"
};
function ThemeToggle() {
	const { activeTheme, setActiveTheme } = useTheme();
	const [pop, setPop] = (0, import_react.useState)(false);
	const handleClick = () => {
		setPop(true);
		setTimeout(() => setPop(false), 220);
		const idx = themeOrder.indexOf(activeTheme);
		setActiveTheme(themeOrder[(idx + 1) % themeOrder.length]);
	};
	return /* @__PURE__ */ (0, import_jsx_runtime.jsx)("button", {
		onClick: handleClick,
		"aria-label": "Toggle theme",
		className: "relative rounded-full",
		style: {
			width: 56,
			height: 28,
			backgroundColor: toggleBg[activeTheme],
			boxShadow: "inset 0 1px 2px rgba(0,0,0,0.12), 0 4px 14px rgba(0,0,0,0.06)",
			transition: "background-color 320ms ease"
		},
		children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("div", {
			style: {
				position: "absolute",
				top: 4,
				left: pop ? "32px" : "4px",
				width: 20,
				height: 20,
				borderRadius: "9999px",
				backgroundColor: themes[activeTheme].accent,
				boxShadow: "0 2px 6px rgba(0,0,0,0.18)",
				display: "flex",
				alignItems: "center",
				justifyContent: "center",
				fontSize: 11,
				transform: pop ? "scale(1.08)" : "scale(1)",
				transition: "left 320ms cubic-bezier(.34,1.56,.64,1), transform 220ms ease, background-color 320ms ease"
			},
			children: /* @__PURE__ */ (0, import_jsx_runtime.jsx)("span", {
				style: {
					display: "inline-block",
					animation: "theme-icon-spin 380ms ease-out"
				},
				children: themeIcons[activeTheme]
			})
		}, activeTheme)
	});
}
//#endregion
export { ThemeProvider, ThemeToggle, themeOrder, themes, useTheme };
